<h1>Clothes in our Website:</h1>

{{#each item}}
    <section >
        <h2>{{this.title}}</h2>
        <p>
            Images: <img src="{{this.image}}" alt=""> <br />
           Price: {{this.price}} <br />
            
            
            
       </p>
<a href="/item/{{this._id}}/edit">Edit</a>
        <form action="/item/{{_id}}/delete" method="POST">
            <button>Delete</button>
        </form>
        <a href="/item/{{this._id}}"> See Details</a>

    </section>
{{/each}}